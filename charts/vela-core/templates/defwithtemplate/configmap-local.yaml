# Code generated by KubeVela templates. DO NOT EDIT. Please edit the original cue file.
# Definition source cue file: vela-templates/definitions/internal/configmap-local.cue
apiVersion: core.oam.dev/v1beta1
kind: SourceDefinition
metadata:
  annotations:
    definition.oam.dev/cache-key-rules: 6ac674fa
    definition.oam.dev/cache-key-rules-version: v1
    definition.oam.dev/description: Reads a ConfigMap's data from the Application's own namespace on the cluster being rendered for. Values are strings, as Kubernetes stores them.
  name: configmap-local
  namespace: {{ include "systemDefinitionNamespace" . }}
spec:
  schematic:
    cue:
      template: |
        import "vela/kube"

        // Generated from the context this template reads - do not edit. Admission re-derives these and rejects a mismatch.
        $internal: {
        	key: "configmap-local-\(context.cluster)-\(context.namespace)"
        	keyInputs: ["cluster", "namespace"]
        }
        // A ConfigMap's data, from this Application's own namespace.
        //
        // Identical to `configmap` bar what it will not do: no `namespace` parameter and
        // no `cluster` parameter, so a component can only ever read a ConfigMap sitting
        // alongside it. That is the difference between "read my config" and "read any
        // namespace's config on any cluster", two capabilities that had been wearing one
        // name.
        //
        // Two definitions rather than one flag, because a parameter cannot be taken
        // away. A `restrictToNamespace: true` field is only as good as the Application
        // author setting it, and the author is exactly who the restriction is about.
        // Binding a Definition is the thing RBAC and Definition permissions can see, so
        // the boundary has to be the Definition rather than a field inside it.
        //
        // A platform that wants tenants reading only their own config grants this one
        // and withholds `configmap`. Nothing here needs configuring to be safe, which is
        // the point: the safe default should not be the one that takes work.
        //
        // Values are strings, as Kubernetes stores them - a ConfigMap holding
        // `replicas: "3"` gives you "3", not 3. The keys are not known here, so `data`
        // is an open map, and a read of one carries the usual obligation to supply a
        // default or be refused at admission where the target is required.
        schema: {
        	data: [string]: string
        }

        storage: {
        	// Same as `configmap`: the read is cheap and local, but re-reading it on
        	// every reconcile of every component is still waste.
        	storageTTL:     "5m"
        	onStaleFailure: "use-stale"
        }

        parameter: {
        	// +usage=Name of the ConfigMap, read from the Application's own namespace
        	name: string
        }

        // Both coordinates come from context, so neither is expressible by the
        // Application. The generated cache key is per-cluster and per-namespace for the
        // same reason it is on `configmap`, and here that is the whole story: with no
        // parameters left to distinguish, two Applications in one namespace asking for
        // the same name genuinely are asking the same question.
        _cm: kube.#Get & {
        	$params: {
        		cluster: context.cluster
        		resource: {
        			apiVersion: "v1"
        			kind:       "ConfigMap"
        			metadata: {
        				name:      parameter.name
        				namespace: context.namespace
        			}
        		}
        	}
        }

        output: data: _cm.$returns.data

