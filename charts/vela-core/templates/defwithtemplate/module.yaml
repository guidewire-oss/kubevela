# Code generated by KubeVela templates. DO NOT EDIT. Please edit the original cue file.
# Definition source cue file: vela-templates/definitions/internal/module.cue
apiVersion: core.oam.dev/v1beta1
kind: ComponentDefinition
metadata:
  annotations:
    definition.oam.dev/description: 'Install a module: fetch it and render its owned Application (XRD, Compositions, definitions) with health-gated tier ordering.'
  name: module
  namespace: {{ include "systemDefinitionNamespace" . }}
spec:
  schematic:
    cue:
      template: |
        import (
        	"vela/module"
        )
        _render: module.#Render & {
        	$params: {
        		module:    parameter.module
        		registry:  parameter.registry
        		namespace: parameter.namespace
        	}
        }

        output: _render.$returns.application

        parameter: {
        	// Module name; defaults to the component name.
        	module: *context.name | string
        	// Registry name; empty means the configured default.
        	registry: *"" | string
        	// Install namespace; empty means the default system namespace (vela-system).
        	namespace: *"" | string
        }
  workload:
    type: autodetects.core.oam.dev

